<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Net_swag_ordering_app.Net_swagitemPage">
             Title = {Binding Name}=>
    <ContentPage.Content>
        
            <StackLayout Margin="20"
                 VerticalOptions="StartAndExpand">
                <Label Text="Name" />
                <Entry Text="{Binding Name}" />
                <Label Text="Gender" />
                <Entry Text="{Binding Gender}" />
                <Label Text="T-shirt Size" />
                <Entry Text="{Binding T-shirt Size}" />
                <Label Text="Date of Order" />
                <Entry Text="{Binding Date of Order}" />
                <Label Text="T-shirt Colour" />
                <Entry Text="{Binding T-shirt Colour}" />
                <Label Text="Shipping Address" />
                <Entry Text="{Binding Shipping Address}" />
                <Switch IsToggled="{Binding}" />
                <Button Text="Save"
                Clicked="OnSaveClicked" />
                <Button Text="Delete"
                Clicked="OnDeleteClicked" />
                <Button Text="Cancel"
                Clicked="OnCancelClicked" />

            </StackLayout>
    </ContentPage.Content>
</ContentPage>